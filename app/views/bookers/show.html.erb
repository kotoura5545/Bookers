<p class="show_title">
	<% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>
	<strong>Title:</strong> <%= @book.title %>
</p>
<p>
	<strong>Body:</strong> <%= @book.body %>
</p>
<%= link_to "Edit", edit_booker_path(@book), class: "edit_#{:id}" %> |
<%= link_to "Back", bookers_path, class: "back" %>
